# Optional in dev: leave unset to use Vite proxy (same tab, no CORS). Set for direct API calls.
# Production / preview: required, e.g. https://your-api.onrender.com
# VITE_API_URL=http://localhost:8000
